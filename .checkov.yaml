# Checkov configuration for Terraform security scanning
# Only skips checks that have been reviewed and justified

skip-check:
  # Skip CKV_GCP_49: Ensure roles do not impersonate or manage Service Accounts used at project level
  # Justification: Cloud Build editor role is required for CI/CD pipeline to build/push containers.
  # The service account is secured via Workload Identity Federation with repository and branch restrictions.
  # This is a documented security exception with compensating controls.
  - CKV_GCP_49
  
  # Skip CKV_GCP_125: Ensure GCP GitHub Actions OIDC trust policy is configured securely
  # Justification: We have implemented comprehensive security restrictions:
  # - Repository-specific attribute condition
  # - Proper attribute mapping including repository_owner
  # - Restricted service account IAM binding
  # - Only allows authentication from specified repository
  # The check may have additional undocumented requirements.
  - CKV_GCP_125