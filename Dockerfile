# syntax=docker/dockerfile:1.2
FROM python:latest
# put you docker configuration here

# Dockerfile under construction. I won't send it, but wit more time, I would probably put my model in a container, depending on where do we want to deploy it.