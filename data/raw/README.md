# Raw Data

This directory contains raw input data files.

## Important Notes
- Never commit data files to version control
- Use only for local development and testing
- Raw data should be processed and moved to data/processed/
- Maintain data lineage documentation

The main dataset (data.csv) is currently in the data/ root directory.