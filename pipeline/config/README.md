# Pipeline Configurations

This directory contains configuration files for pipeline execution.

## Configuration Structure
Configurations use YAML format with hierarchical overrides:
- Global settings
- Environment-specific settings (dev, stage, prod)
- Runtime settings

All configurations must follow the YAML hierarchical override pattern specified in the constitution.