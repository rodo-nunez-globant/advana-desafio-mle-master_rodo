# Source Code Modules

This directory contains the main source code for the project, organized by functionality:

- `data/` - Data processing and loading modules
- `features/` - Feature engineering modules
- `models/` - Model definition and training modules
- `evaluation/` - Model evaluation and metrics modules
- `utils/` - Utility functions and helpers

All modules should follow SOLID principles and include proper type hints and docstrings.