# Data Processing Modules

This directory contains modules for data loading, preprocessing, and validation.

## Expected Modules
- Data loaders for different formats (CSV, JSON, etc.)
- Data validation and quality checks
- Preprocessing pipelines
- Data transformation utilities

All data processing must prevent data leakage by performing train/test splits before any transformations.